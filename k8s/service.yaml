apiVersion: v1
kind: Service
metadata:
  name: e2e-react-azure
  namespace: sandbox-e2e-react-azure
  labels:
    app: e2e-react-azure
    tenant: mastercard
spec:
  type: ClusterIP
  selector:
    app: e2e-react-azure
  ports:
    - name: http
      port: 80
      targetPort: 8080
